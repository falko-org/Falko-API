version: '3'
services:

  falko-database:
    container_name: falko-database
    image: postgres
    ports:
      - 5000:5432
    environment:
      - PGDATA=/postgresql
      - PG_PASSWORD=password
    volumes:
      - ./tmp/data/postgresql:/postgresql
      - .:/Falko-2017.2-BackEnd

  falko-server:
    container_name: falko-server
    image: alaxalves/api:1.2
    command: bundle exec rails s -p 3000 -b '0.0.0.0'
    volumes:
      - .:/Falko-2017.2-BackEnd
    ports:
      - 3000:3000
    depends_on:
      - falko-database
